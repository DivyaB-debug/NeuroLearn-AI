# AI provider keys (required)
GOOGLE_GENERATIVE_AI_API_KEY=
OPENAI_API_KEY=

# Optional: Render PostgreSQL connection string (only needed if you add server-side accounts/progress)
DATABASE_URL=

# NODE_ENV is set by the build/runtime environment. Do not hardcode it here.
