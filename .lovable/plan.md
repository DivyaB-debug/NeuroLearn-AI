The 'Edit with Lovable' badge is currently visible on published deployments. I will hide it by updating the project's badge visibility setting. This is a single settings change — no code edits required.

**Action:**
- Call `publish_settings--set_badge_visibility` with `hide_badge: true`.

**Note:** Hiding the badge may require a paid Lovable plan. If the request fails for billing reasons, I will let you know and suggest alternatives.